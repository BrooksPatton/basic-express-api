{
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4",
    "morgan": "^1.7.0",
    "nodemon": "^1.9.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "supertest": "^1.2.0"
  },
  "scripts": {
    "test": "export NODE_ENV=test && mocha",
    "start": "export NODE_ENV=prod && node bin/server.js",
    "dev": "export NODE_ENV=dev && nodemon bin/server.js"
  },
  "name": "basic-express-api",
  "version": "0.0.0"
}
